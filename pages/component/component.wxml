<view>
  <block wx:for="{{list}}" wx:for-item="item" wx:key="index">
    <view class="cellStyle" bindtap="turnToDemo" data-component="{{item.component}}">
      [{{index}}]{{item.title}}
    </view>
  </block>
</view>